package com.lifelinecalllog.controller;

// SPA routing is handled by WebConfig's addViewControllers fallback to index.html.
// No forwarding needed here — forwarding caused infinite dispatch loops with Spring MVC.
